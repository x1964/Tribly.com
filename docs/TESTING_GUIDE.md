# دليل الاختبار السريع - Profile Page

## 🚀 ابدأ الآن:

### 1. افتح الصفحة
```
اذهب إلى: profile.html
```

### 2. ملء البيانات الأساسية
```
✅ الاسم: أحمد محمد
✅ العمر: 25
✅ الجنس: ذكر
✅ المدينة: القاهرة
✅ البريد: ahmed@example.com
✅ النبذة: أنا مهندس برمجيات...
```

### 3. إضافة اهتمامات
```
اكتب: "الموسيقى" واضغط Enter
اكتب: "السفر" واضغط Enter
اكتب: "القراءة" واضغط Enter
```

### 4. رفع صورة
```
اضغط على الصورة الشخصية
اختر صورة من جهازك
ستظهر الصورة فوراً
```

### 5. ضبط التفضيلات
```
✅ نطاق العمر من: 20 إلى 30
✅ تبحث عن: الاثنين
✅ المسافة: 50 كم
```

### 6. حفظ البيانات
```
اضغط زر "حفظ التغييرات"
ستظهر رسالة النجاح
البيانات محفوظة في localStorage
```

---

## 🧪 اختبارات الأخطاء:

### اختبار 1: اسم فارغ
```
اترك حقل الاسم فارغاً
اضغط حفظ
النتيجة: ⚠️ رسالة خطأ
```

### اختبار 2: عمر خاطئ
```
أدخل عمر أقل من 18
اضغط خارج الحقل
النتيجة: 🔴 الحد يتحول أحمر
```

### اختبار 3: بريد خاطئ
```
أدخل: invalid
اضغط خارج الحقل
النتيجة: 🔴 الحد يتحول أحمر
```

### اختبار 4: اهتمام مكرر
```
أضف اهتمام مرتين
النتيجة: ⚠️ رسالة: هذا الاهتمام موجود
```

### اختبار 5: صورة كبيرة
```
حاول رفع صورة > 5MB
النتيجة: ⚠️ رسالة: حجم كبير جداً
```

---

## 💡 اختبارات الوظائف:

### اختبار localStorage
```
افتح Developer Tools (F12)
اذهب إلى Application > localStorage
ابحث عن: userProfile, userAvatar
النتيجة: ✅ البيانات محفوظة
```

### اختبار التحديث التلقائي
```
راقب الأرقام في الشريط الجانبي (إعجابات، توافقات، مشاهدات)
انتظر 30 ثانية
النتيجة: ✅ الأرقام تتغير تلقائياً
```

### اختبار الخروج
```
اضغط "خروج" من القائمة
اختر "نعم" من التأكيد
النتيجة: ✅ ينتقل إلى login.html
```

---

## 📱 اختبار الاستجابة:

### على سطح المكتب
```
بحجم عادي (1200px+)
النتيجة: ✅ شريط جانبي + محتوى
```

### على التابلت
```
اضغط F12 > Toggle device toolbar
اختر iPad
النتيجة: ✅ عمود واحد
```

### على الهاتف
```
اختر iPhone SE
النتيجة: ✅ عمود واحد + نصوص كبيرة
```

---

## 🔍 اختبارات Console:

### فتح Console
```
F12 > Console tab
```

### اختبر الوظائف:
```javascript
// إضافة اهتمام
addInterest()

// الحصول على الاهتمامات
getInterestsList()

// تحديث الإحصائيات
updateStats()

// عرض البيانات المحفوظة
console.log(JSON.parse(localStorage.getItem('userProfile')))

// حذف البيانات (للاختبار)
localStorage.clear()
```

---

## ✅ قائمة التحقق النهائية:

### البيانات الأساسية
- [ ] الاسم يحفظ بشكل صحيح
- [ ] العمر يتحقق من الصحة
- [ ] البريد يتحقق من الصيغة
- [ ] الجنس مختار
- [ ] النبذة محفوظة

### الاهتمامات
- [ ] يمكن إضافة اهتمامات
- [ ] يمنع التكرار
- [ ] يمكن الحذف
- [ ] يعمل مفتاح Enter
- [ ] حد أقصى 10 اهتمامات

### الصورة
- [ ] يمكن رفع صورة
- [ ] تظهر معاينة فورية
- [ ] تحفظ في localStorage
- [ ] تحمل عند الدخول

### التفضيلات
- [ ] نطاق العمر يحفظ
- [ ] نوع العلاقة يحفظ
- [ ] نطاق المسافة يحفظ

### الأمان
- [ ] لا توجد ثغرات XSS
- [ ] التحقق من الملفات يعمل
- [ ] رسائل الخطأ واضحة

### الاستجابة
- [ ] يعمل على سطح المكتب
- [ ] يعمل على التابلت
- [ ] يعمل على الهاتف

### الأداء
- [ ] الصفحة تحمل بسرعة
- [ ] لا توجد أخطاء في Console
- [ ] التحديثات سلسة

---

## 📞 المساعدة:

إذا واجهت مشكلة:
1. افتح F12 Console
2. ابحث عن الأخطاء الحمراء
3. تحقق من localStorage
4. جرب تحديث الصفحة (Ctrl+R)
5. جرب حذف السجل (Ctrl+Shift+Delete)

---

## 🎉 النتيجة:

إذا نجحت جميع الاختبارات = ✅ الصفحة تعمل بكفاءة 100%

Good luck! 🚀
