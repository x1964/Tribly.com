# نظام الترجمة المتعدد اللغات - Tribly Translation System

## 🌍 اللغات المدعومة | Supported Languages

- 🇸🇦 **العربية** (Arabic) - `ar`
- 🇬🇧 **الإنجليزية** (English) - `en`
- 🇫🇷 **الفرنسية** (French) - `fr`

## 📋 كيفية استخدام نظام الترجمة | How to Use

### 1. إضافة نص قابل للترجمة | Adding Translatable Text

في ملفات HTML، استخدم `data-i18n` attribute:

```html
<h1 data-i18n="how.title">How Does It Work?</h1>
<p data-i18n="how.subtitle">Find your people...</p>
```

### 2. إضافة ترجمات جديدة | Adding New Translations

في ملف `js/language.js`، أضف مفتاح جديد لكل لغة:

```javascript
// في القسم الخاص بكل لغة:
'your.new.key': 'النص العربي',  // ar
'your.new.key': 'English Text',  // en
'your.new.key': 'Texte Français',  // fr
```

### 3. التبديل بين اللغات | Switching Languages

يوجد زر تبديل لغة في شريط التنقل (Navbar):
- اضغط على **AR** للعربية
- اضغط على **EN** للإنجليزية
- اضغط على **FR** للفرنسية

اللغة تُحفظ تلقائياً في `localStorage` وتُستعاد عند العودة للموقع.

## 🗂️ هيكل نظام الترجمة | System Structure

```
js/
├── language.js          ← نظام الترجمة الرئيسي
├── firebase.js
├── main.js
└── ...
```

### ملف language.js

يحتوي على:
- `LanguageManager` class - إدارة اللغات
- `translations` object - جميع الترجمات (AR, EN, FR)
- `setLanguage()` - تغيير اللغة
- `get()` - الحصول على ترجمة محددة
- `applyLanguage()` - تطبيق الترجمة على الصفحة

## 🎯 ميزات النظام | Features

✅ **تبديل فوري** - التبديل بين اللغات بدون إعادة تحميل الصفحة
✅ **اتجاه ديناميكي** - RTL للعربية، LTR للإنجليزية والفرنسية
✅ **حفظ التفضيل** - اللغة تُحفظ في localStorage
✅ **سهل الإضافة** - إضافة لغات جديدة سهلة جداً

## 📝 مثال عملي | Practical Example

### HTML
```html
<section id="cities">
  <h2 data-i18n="cities.title">Where We're Growing</h2>
  <p data-i18n="cities.expanding">Currently active...</p>
</section>
```

### JavaScript (في language.js)
```javascript
ar: {
  'cities.title': 'حيث ننمو',
  'cities.expanding': 'نشط حالياً في أكثر مدن...',
},
en: {
  'cities.title': 'Where We\'re Growing',
  'cities.expanding': 'Currently active in...',
},
fr: {
  'cities.title': 'Où nous grandissons',
  'cities.expanding': 'Actuellement actif dans...',
}
```

## 🔄 الحفظ التلقائي | Auto-Save

```javascript
// اللغة المختارة تُحفظ تلقائياً:
localStorage.setItem('language', lang);

// وتُحمل عند فتح الموقع:
const currentLanguage = localStorage.getItem('language') || 'ar';
```

## 📱 الاستجابة للتغييرات | Event Handling

عند تغيير اللغة:
```javascript
window.addEventListener('languageChanged', () => {
  // إعادة تحميل أو تحديث أي محتوى ديناميكي
});
```

## ⚙️ التخصيص | Customization

### إضافة لغة جديدة

1. أضف في `js/language.js`:
```javascript
this.translations = {
  ar: {...},
  en: {...},
  fr: {...},
  es: {  // إضافة لغة جديدة
    'key': 'Valor español'
  }
};
```

2. أضف الزر في `createLanguageSwitcher()`:
```javascript
const languages = ['ar', 'en', 'fr', 'es']; // أضف اللغة
```

## 🐛 استكشاف الأخطاء | Troubleshooting

### النص لا يتغير عند التبديل
- تأكد من استخدام `data-i18n="key.name"`
- تحقق من وجود المفتاح في جميع لغات الترجمة

### اتجاه النص غير صحيح
- تحقق من `document.documentElement.dir`
- يجب أن يكون `rtl` للعربية و `ltr` للإنجليزية والفرنسية

### اللغة لا تُحفظ
- تحقق من أن localStorage مفعل
- تأكد من عدم وجود مشاكل في الكونسول

## 📊 إحصائيات الترجمات | Translation Stats

| اللغة | عدد المفاتيح |
|------|---------|
| العربية (AR) | 100+ |
| الإنجليزية (EN) | 100+ |
| الفرنسية (FR) | 100+ |

## 🚀 الخطوات التالية | Next Steps

1. ✅ نظام الترجمة الأساسي متكامل
2. 📝 إضافة المزيد من الترجمات للصفحات الأخرى
3. 🌐 إضافة لغات جديدة (إسباني، ألماني، إلخ)
4. 📊 تتبع كيفية تفضل المستخدمين لكل لغة

---

**آخر تحديث**: January 22, 2026
**الإصدار**: v1.0.0
