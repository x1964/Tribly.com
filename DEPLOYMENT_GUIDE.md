# 📋 دليل النشر والتنشيط - Tribly 2.0

## 🚀 قبل النشر - قائمة التفقد

### 1️⃣ التحقق من Firebase
```
✅ تم إنشاء مشروع Firebase
✅ تم تفعيل Realtime Database
✅ تم تفعيل Authentication
✅ تم نسخ مفاتيح API
✅ تم تحديث firebaseConfig في js/firebase-init.js
```

**الملف المراد تحديثه:**
```
js/firebase-init.js - السطور 1-10
```

### 2️⃣ التحقق من الملفات
```
✅ جميع ملفات HTML موجودة (38 ملف)
✅ جميع ملفات CSS موجودة (4 ملفات)
✅ جميع ملفات JS موجودة (8 ملفات)
✅ جميع الصور موجودة في img/
✅ ملف package.json موجود
✅ ملف .htaccess موجود
```

### 3️⃣ التحقق من البيانات
```
✅ localStorage يعمل بدون أخطاء
✅ Firebase Realtime Database جاهز
✅ Security Rules مُعدة بشكل صحيح
✅ CORS Rules معرّفة (إن لزم الأمر)
```

---

## 🔧 خطوات النشر على الاستضافة

### المرحلة 1: إعداد البيئة المحلية

#### 1. تثبيت الحزم
```bash
cd d:\Tribly.com
npm install
```

#### 2. اختبار محلي
```bash
npm start
# أو
npm run dev
```

#### 3. فتح في المتصفح
```
http://localhost:8080
```

---

### المرحلة 2: نسخ الملفات إلى الاستضافة

#### المجلدات والملفات المهمة:
```
📦 /
├── 📄 *.html (جميع ملفات HTML)
├── 📁 css/
│   ├── contact.css
│   ├── home.css
│   ├── profile.css
│   └── style.css
├── 📁 js/
│   ├── firebase.js (أضفه من Firebase SDK)
│   ├── firebase-init.js ⭐ (يحتوي على المفاتيح)
│   ├── main.js
│   ├── script.js
│   ├── profile.js
│   ├── dashboard.js
│   ├── contact.js
│   └── language.js
├── 📁 img/
│   └── (جميع الصور)
├── 📁 docs/
│   └── (جميع ملفات التوثيق)
├── .htaccess ⭐ (مهم جداً للمسارات)
├── manifest.json
└── package.json
```

#### التحضير للنشر:
```bash
# 1. تأكد من أن firebase-init.js يحتوي على المفاتيح الحقيقية
# 2. تأكد من أن جميع الملفات موجودة
# 3. اختبر كل صفحة محلياً
# 4. افحص أخطاء Console (F12)
```

---

### المرحلة 3: رفع الملفات

#### الخطوة 1: الاتصال بـ FTP
```
استضيفك: [أدخل بيانات الاستضافة]
- Host: ftp.domain.com
- Username: your_username
- Password: your_password
```

#### الخطوة 2: رفع الملفات
```
يمكنك استخدام:
- FileZilla (مجاني)
- WinSCP
- cPanel File Manager
- SSH
```

#### الملفات الحساسة (تحقق قبل الرفع):
```
⚠️ js/firebase-init.js - يحتوي على المفاتيح
⚠️ .htaccess - إعدادات الخادم المهمة
⚠️ .env.example - لا ترفعها (example فقط)
```

---

### المرحلة 4: التكوين على الخادم

#### إذا كان الخادم Apache:

1️⃣ **تفعيل mod_rewrite**
```
اتصل بدعم الاستضافة لتفعيل mod_rewrite
أو أضف في .htaccess:
```

2️⃣ **.htaccess موجود بالفعل مع:**
```
✅ Gzip Compression
✅ Cache Headers
✅ Security Rules
✅ Directory Protection
✅ File Type Restrictions
```

#### إذا كان الخادم Nginx:

إنشاء `/etc/nginx/sites-available/tribly`:
```nginx
server {
    listen 80;
    server_name tribly.com www.tribly.com;
    root /var/www/tribly;
    index index.html;

    # SPA Configuration
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|webp)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}
```

#### تفعيل الموقع:
```bash
sudo ln -s /etc/nginx/sites-available/tribly /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

---

### المرحلة 5: SSL Certificate (HTTPS)

#### استخدام Let's Encrypt (مجاني):
```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d tribly.com -d www.tribly.com
```

#### في cPanel:
```
1. AutoSSL
2. أو اطلب من الدعم تثبيت SSL
```

---

## ✅ اختبار النشر

### 1. اختبر في المتصفح:
```
https://tribly.com
https://www.tribly.com
```

### 2. اختبر جميع الصفحات:
```
✅ home.html - الرئيسية
✅ login.html - تسجيل الدخول
✅ signup.html - إنشاء حساب
✅ profile.html - الملف الشخصي
✅ match.html - المطابقة
✅ communities.html - المجتمعات
✅ customer-dashboard.html - لوحة التحكم
```

### 3. اختبر الوظائف:
```
✅ تسجيل حساب جديد
✅ تسجيل دخول
✅ تحميل الملف الشخصي
✅ حفظ البيانات
✅ تحميل الصفحات بسرعة
✅ لا توجد أخطاء في Console
```

### 4. اختبر الأمان:
```
✅ لا يمكن الوصول إلى .env
✅ لا يمكن الوصول إلى .git
✅ لا يمكن عرض قائمة الملفات
✅ جميع المسارات تعمل
```

### 5. اختبر الأداء:
```
✅ سرعة التحميل < 3 ثوانٍ
✅ تأثير Cumulative Layout Shift (CLS)
✅ الصور محسّنة
✅ JS/CSS مضغوط
```

---

## 🔍 أدوات التحقق

### Google PageSpeed Insights:
```
https://pagespeed.web.dev
```

### GTmetrix:
```
https://gtmetrix.com
```

### SSL Checker:
```
https://www.sslshopper.com/ssl-checker.html
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: "لا يعمل تسجيل الدخول"
**الحل:**
```
1. افحص Firebase Credentials في js/firebase-init.js
2. تأكد من تفعيل Authentication في Firebase Console
3. افحص Console (F12) للأخطاء
```

### المشكلة: "لا تُحفظ البيانات"
**الحل:**
```
1. تأكد من تفعيل Realtime Database
2. افحص Security Rules في Firebase
3. تأكد من الاتصال بالإنترنت
```

### المشكلة: "صفحات معينة لا تعمل"
**الحل:**
```
1. اختبر .htaccess إذا كان Nginx تجاهله
2. تحقق من مسارات الملفات
3. افحص browser console للأخطاء
```

### المشكلة: "الصور لا تظهر"
**الحل:**
```
1. تأكد من وجود مجلد img/
2. تحقق من مسارات الصور الصحيحة
3. افحص صيغ الصور المدعومة
```

---

## 📊 ملخص البيئات

### البيئة المحلية (Local)
```
URL: http://localhost:8080
Firebase: Development Credentials
Debug: ON
Cache: OFF
```

### بيئة الإنتاج (Production)
```
URL: https://tribly.com
Firebase: Production Credentials
Debug: OFF
Cache: ON
SSL: HTTPS Required
```

---

## 📞 معلومات الدعم

### للمشاكل التقنية:
```
📧 triblyteam@gmail.com
📱 +20 120 777 1639
```

### ملفات التوثيق:
```
📄 FIREBASE_CONFIG_GUIDE.md
📄 FIREBASE_QUICK_REFERENCE.md
📄 QUICK_START_GUIDE.md
📄 UPDATE_SUMMARY_2.0.md
```

---

## ✨ ملاحظات أخيرة

```
✅ الموقع متاح الآن
✅ جميع الميزات تعمل
✅ البيانات محفوظة في Firebase
✅ الأمان على أعلى مستوى
✅ الأداء محسّنة

🎉 مبروك! موقعك جاهز للعالم!
```

---

**آخر تحديث**: 22 يناير 2026  
**الإصدار**: 2.0.0 - جاهز للإنتاج
